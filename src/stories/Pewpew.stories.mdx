import { Meta, Story } from "@storybook/addon-docs";
import { PewPew } from "..";
import JSZip from "jszip";

<Meta title="PewPew" component={PewPew} />
# `<PewPew/>` component
  A JSZip instance containing the level data is passed to the story as the `level` prop.<br/> 
  Any other props are passed down to the iFrame (including `style`,`className`,etc).
<Story
  name="Primary">
  {(args, { loaded: { level } }) => (
    <PewPew {...args} level={level} style={{width:"100%",height:"90vh"}} />
  )}
</Story>
 

